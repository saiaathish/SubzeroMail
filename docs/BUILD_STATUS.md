# Build Status

Source of truth: [`PRD.md`](./PRD.md). This ledger separates local implementation evidence from live Gmail/provider release gates. A `PASS (local/demo)` row is not a production or hosted-verification claim.

## Delivery state

| PRD ID                                         | Status                     | Evidence / blocker                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Foundation                                     | PASS (local)               | `npm test`: 23 files / 119 tests passed. `npm run typecheck`, clean `npm run build`, and `npm run format:check` passed.                                                                                                                                                                                                                                                                                                      |
| P0.1 / FR-001 Gmail connect + sync             | PASS (live)                | Real OAuth round trip completed in Chrome against Google: test-user consent granted for `gmail.modify`, callback exchanged the code, and the account (`saiaathishk@gmail.com`) was stored with an encrypted refresh token. Live sync: 200 active threads loaded. Reconnect/revoke paths remain covered by unit tests.                                                                                                        |
| P0.2 / FR-002 / FR-004 Inbox shell + mutations | PASS (live)                | Three-zone inbox rendered from live Gmail; archive, read/unread, and STARRED mutations round-tripped to the Gmail API and re-verified directly via API (label sets before/after each mutation); cached inbox survived a full reload; optimistic rollback/reconcile remains covered by tests.                                                                                                                                 |
| P0.3 / FR-003 Keyboard workflow                | PASS (local/demo + Chrome) | Core shortcuts, focus behavior, composer typing guard, revoked-auth reconnect, prompt-injection safety: Chrome 14/14 and WebKit 14/14 demo E2E, 12/12 smoke. Per maintainer decision, Firefox/Edge are dropped as release gates; Chrome infrastructure coverage is the release bar.                                                                                                                                          |
| P0.4 / FR-005 Focus triage                     | PASS (live)                | Deterministic buckets computed live over the real inbox (Priority 111 / Needs Reply 11 / Waiting 2 / Other 76 with reasons); classification flipped correctly after live read-state mutations; manual override combobox present; AI refine runs on the verified provider.                                                                                                                                                    |
| P0.5 / FR-006 Thread summaries                 | PASS (live)                | Evidence-backed 3-sentence summary plus latest-change/action/deadline generated from a real Devpost email via `deepseek-v4-flash` (OpenCode Zen Go), cached by latest message; stale-summary invalidation and recoverable provider errors remain covered by tests.                                                                                                                                                           |
| P0.6 / FR-007 Compose + reply                  | PASS (live)                | Draft created (`POST /api/mail/drafts` 201) and sent (`/send` 200) through the Gmail API to the account owner; the sent thread appeared in the live inbox with SENT+INBOX labels; explicit-send confirmation is in the composer; reply/reply-all recipient handling and failure recovery remain covered by tests.                                                                                                            |
| P0.7 / FR-008 AI drafting                      | PASS (live)                | Intent → editable draft via `deepseek-v4-flash`; the draft used thread context (addressed the organizer by name) and covered every intent point; manual compose/send verified separately; streaming/cancel/regenerate and provider-outage behavior remain covered by demo tests.                                                                                                                                             |
| P0.8 / FR-009 Search + palette                 | PASS (live)                | `from:uber` Gmail query returned 44 real threads and the result opened the correct thread; keyboard command palette and keyboard accessibility remain covered by Chrome demo tests.                                                                                                                                                                                                                                          |
| FR-013 BYOK                                    | PASS (live)                | Provider key stored encrypted at rest (verified in SQLite), never re-displayed, add/test/remove flow works; OpenAI-compatible provider now supports a custom base URL — verified live against OpenCode Zen Go (`https://opencode.ai/zen/go/v1`, model `deepseek-v4-flash`); invalid-key and malformed-URL errors produce useful UI (both verified live).                                                                     |
| P1.1 / FR-010 Ask Inbox                        | PASS (live)                | Bounded retrieval over the real mailbox: answer listed registered hackathons with the next kickoff, showed 5 source message IDs, and honestly stated where evidence was missing. Unit/integration and demo acceptance also pass.                                                                                                                                                                                             |
| P1.2 / FR-011 Open Loops                       | PASS (live)                | Source-backed extraction from a real email produced two loops with correct due dates (Discord join by 2026-08-14; project submission by 2026-08-16), source links, and edit controls. Deduplication and edit/resolve routes remain covered by unit/integration tests.                                                                                                                                                        |
| P1.2 Reminders                                 | PASS (live + tests)        | Open commitments due now or within 2 days resurface in the Open Loops panel as overdue/due-soon reminders with "Open thread" and "Mark resolved". Automatic resolution is direction-aware: an `i_owe` loop resolves only when the account owner replied; `they_owe`/`waiting` only when the other party replied (an inbound nudge never clears the user's own obligation). Independent red-team review confirmed and tested. |
| P1 Auto Drafts                                 | PASS (tests)               | Background suggested draft for an opened high-priority (Needs Reply / Waiting) thread: generated once per thread, cached and reused while unchanged, regenerated only when a newer message arrives, and never overwritten or regenerated after the user edits (server-enforced via `user_edited_at`). Account-scoped; fires at most once per composer session.                                                               |
| P1 Auto Archive                                | PASS (tests)               | Opt-in (default off) deterministic auto-archive: threads with the newsletter signal (confidence ≥ 0.90) skip the inbox on plain inbox loads — never on search keystrokes. Server-enforced 403 gate when disabled; the client shows "Auto-archived N" with Gmail All Mail recovery note. Independent red-team review confirmed the opt-in gate and deterministic-only signal.                                                 |
| P1.3 / FR-012 Voice Profile                    | PASS (local/demo)          | Explicit opt-in, bounded 20-message sample, compact editable/resettable profile, no raw sample persistence, and unit/integration/UI coverage pass. Drafting through the verified provider path is covered by demo acceptance.                                                                                                                                                                                                |
| P1.4 Custom Focus rules                        | PASS (local/demo)          | Inspectable deterministic rules, validation/de-duplication, account scoping, unit/integration coverage, and Chrome/WebKit demo acceptance pass.                                                                                                                                                                                                                                                                              |
| P1.5 Snooze                                    | CUT / DEFERRED             | No implementation is present. Gmail snooze semantics are not a low-cost slice in this release; no fragile substitute is shipped. Revisit only after P0/P1 stability and an explicit scope decision.                                                                                                                                                                                                                          |
| P2                                             | CUT / DEFERRED             | Quick replies, snippets, receipts, local models, and embeddings remain outside the current release per the PRD. (Auto-archive moved up to P1 as an opt-in deterministic slice.)                                                                                                                                                                                                                                              |

## External blockers and release limits

- Live Gmail/OAuth verification: RESOLVED. The full OAuth round trip completed with the project's Web client (`Web client 1`, redirect `http://localhost:3000/api/auth/google/callback`), test user `saiaathishk@gmail.com`, and `gmail.modify`. For a public hosted rollout, Google restricted-scope verification of the OAuth app is still required; this release targets configured self-host/test users.
- Live AI verification: RESOLVED. The user-supplied OpenCode Zen Go key authenticates at `https://opencode.ai/zen/go/v1` with model `deepseek-v4-flash`; the BYOK settings dialog now supports a custom base URL for OpenAI-compatible providers. The key is stored encrypted in SQLite and is never printed, logged, or committed by this build.
- Browser evidence: the web demo evidence remains Chrome 14/14 and WebKit 14/14 against isolated production servers, plus 12/12 Chrome smoke runs. The extension-specific Chromium suite is 4/4, covering P0 actions, summaries, Ask Inbox, Open Loops, logout/theme preservation, and popup/full-page loading. Per maintainer decision, Firefox and Edge are dropped as release gates: the Playwright Firefox runner cannot create a usable tab on this macOS host, and Edge requires an installed system binary. Chrome infrastructure coverage is the release bar.
- Performance targets: PASS (local/demo) — 10 Chrome samples measured cached inbox render p50 202 ms (target <300 ms), cached thread open p50 14 ms (target <200 ms), and optimistic archive visual response p50 5 ms (target <100 ms). These are local fixture measurements, not live Gmail measurements.
- The production-configured `npm run test:e2e` command rebuilds the Next artifact before starting its server. Run it from a clean, isolated worktree/build directory; overlapping/stale `.next` output can produce ENOENT failures. The clean build used for this ledger passed, but no full production E2E matrix is claimed.
- Docker tooling is present; `docker compose config`, image build, and isolated container `/api/health` smoke pass. `.dockerignore` reduced the build context from roughly 800 MB to 38 KB. Live container Gmail/OAuth is not claimed. The Docker path still requires a configured `.env`, OAuth test user, encryption key, and optional BYOK key; SQLite data persists in the named `subzero-data` volume.
- A fresh clone of the pushed `main` branch passed `npm ci`, `npm run typecheck`, and `npm run build`. `npm audit --omit=dev` reports 7 transitive advisories (3 high, 4 moderate) in Next/PostCSS, sharp, and googleapis/uuid; the available automated fix requires breaking upgrades, so no forced dependency rewrite was applied.
- Screenshot artifacts: [`docs/screenshots/inbox-demo.png`](screenshots/inbox-demo.png) is a demo/fixture capture; [`docs/screenshots/live-ask-inbox.png`](screenshots/live-ask-inbox.png) is a live capture of Ask Inbox answering over the real mailbox with source chips.
- Independent red-team review (read-only, hostile) of commit `b13e890` APPROVED the release with fixes, all landed: direction-aware auto-resolve (HIGH), base-URL hardening — embedded credentials rejected and plain http limited to loopback hosts (MEDIUM), auto-archive scoped off search keystrokes with an archived-count notice (MEDIUM), ledger accuracy (LOW), and a valid 32-byte e2e encryption key (INFO). The review confirmed BYOK encryption-at-rest, auto-draft takeover semantics, the auto-archive opt-in gate, prompt-injection boundaries, optimistic rollback/reconcile, and test integrity.

## Test ledger

| Gate               | Latest result                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------- |
| Unit + integration | PASS — 23 Vitest files, 119/119 tests                                                             |
| TypeScript         | PASS — `npm run typecheck`                                                                        |
| Production build   | PASS — clean `npm run build`                                                                      |
| Formatting         | PASS — `npm run format:check`                                                                     |
| Browser demo E2E   | PASS — Chrome 14/14 and WebKit 14/14 against isolated production servers; 12/12 Chrome smoke runs |
| Firefox E2E        | DROPPED — per maintainer decision; Playwright Firefox also cannot launch on this macOS runner     |
| Edge E2E           | DROPPED — per maintainer decision; opt-in project requires installed system Edge                  |
| Docker config      | PASS — `docker compose config` with documented placeholder environment                            |
| Docker self-host   | PASS (local/demo) — image build + Compose container `/api/health`; live OAuth not run             |
| Clean checkout     | PASS — fresh clone, `npm ci`, typecheck, and production build                                     |
| Dependency audit   | WARN — 7 transitive advisories; fixes require breaking upgrades                                   |

## Chrome extension and public distribution status

This section is separate from the web-client evidence above. Web `PASS (live)`
rows do not prove extension live Gmail or store behavior. The extension now has
local implementation and fixture-backed browser evidence; OAuth, live Gmail,
and public distribution remain separate gates.

| Gate                                   | Status                                       | Evidence / blocker                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Extension package and workspace        | PASS (local)                                 | `apps/extension` is a WXT React TypeScript workspace with app, popup, side-panel, scoped Gmail content-script, and MV3 background entrypoints. Popup onboarding, typed preferences, and opt-in automation adapters are included.                                                                                                                                                                                                                                                 |
| Extension manifest and permissions     | PASS (local)                                 | `npm run extension:build` emits MV3 with scoped `https://mail.google.com/*` content script, `sidePanel`, `notifications`, Gmail API access, and user-requested optional provider origins; validator checks entrypoints and dynamic-code patterns.                                                                                                                                                                                                                                |
| Extension ID                           | KNOWN (draft item) / MANUAL BLOCKER          | Chrome Web Store draft item ID is `mdiopbbmnkdbkmkadcloadgbedfofdge`. Release signing/verified-upload configuration and a published version remain unverified.                                                                                                                                                                                                                                                                                                                   |
| Extension Google OAuth client          | CONFIGURED (external + local) / MANUAL       | Google Cloud has a dedicated `Subzero Mail Chrome Extension` client whose item ID is wired in `apps/extension/wxt.config.ts`; the repository contains no secret. Consent success, clean-profile live authorization, and restricted-scope review remain unverified.                                                                                                                                                                                                               |
| Exact Google OAuth scopes              | PARTIAL (local manifest)                     | The extension manifest declares `gmail.modify`, `openid`, `email`, and `profile` alongside the configured client ID. Consent, extension-ID/origin alignment, and restricted-scope review remain unverified.                                                                                                                                                                                                                                                                      |
| Public privacy page                    | PASS (public policy URL; web deploy pending) | [`docs/PRIVACY.md`](PRIVACY.md) is publicly reachable at the GitHub policy URL recorded in [`CHROME_STORE_LISTING.md`](CHROME_STORE_LISTING.md) (HTTP 200) and includes `saiaathishk@gmail.com`; the Next `/privacy` deployment origin is still not assigned.                                                                                                                                                                                                                    |
| Chrome Web Store listing               | PARTIAL (saved draft; not submitted)         | Draft item `mdiopbbmnkdbkmkadcloadgbedfofdge` has the package, category/language, icon, three fresh MV3 screenshots, homepage/support links, reviewer instructions, and privacy/data disclosures saved. No review result is claimed.                                                                                                                                                                                                                                             |
| Google restricted-scope verification   | BLOCKED / MANUAL                             | `gmail.modify` is restricted. No current manual verification record for this extension distribution is present. **VERIFY CURRENT DOCS** before submission.                                                                                                                                                                                                                                                                                                                       |
| Extension local/demo/live/manual gates | PASS (local/demo) / BLOCKED (live/manual)    | `npm run extension:test`: MV3 validator PASS and Chromium fixture suite 4/4, including the user-facing Gmail access disclosure gate. `npm run extension:zip` emits `.output/subzeroextension-0.1.0-chrome.zip`. No real Gmail credential, revoke, provider-network, or store-review run is claimed.                                                                                                                                                                              |
| Chrome extension parity                | PASS (local/demo) / PARTIAL (live/manual)    | Full-page client, focus views, keyboard shell, fixture cache, theme, popup onboarding, OAuth boundary, Gmail API search/thread/star/draft/send paths, sanitized HTML, local summaries/drafts, Ask Inbox evidence, Open Loops/reminder scheduling, session-only BYOK settings, opt-in auto-label/archive adapters, and logout are implemented and fixture-tested. Gmail embedded and Side Panel local slices are built/tested; live Gmail/provider/store evidence remains manual. |

### Manual release blockers

Before a Chrome extension release can be called ready, record and verify the
extension artifact/signing choice, the configured extension OAuth client/origin
alignment, final permissions, user-facing Gmail/provider disclosure, Google
restricted-scope review, current Chrome Web Store data-safety requirements,
clean-profile OAuth/revoke/sign-out/deletion tests, and the final store review
result. The public policy URL and real extension screenshots are now saved in
the draft; local implementation is not live-Gmail or store-review evidence.

### Privacy evidence boundary

The current web sanitizer removes active email content but permits sanitized
remote image URLs. The extension fetches selected Gmail thread bodies on
demand, sanitizes them through `@subzero/security/client`, and renders the
sanitized HTML. Remote images remain a sender-controlled network request risk;
the local P0 fixture test proves unsafe active URLs are removed. See
[`PRIVACY.md`](PRIVACY.md).

## Gmail embedded mode

This ledger records the Gmail-native slice separately from the existing
standalone extension evidence. Local PASS means compiled or fixture-tested; it
does not prove stability against Gmail's live DOM or Chrome Web Store review.

| Surface                | Status                        | Evidence / limit                                                                                                                                                                                                                           |
| ---------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Content script         | PASS (local)                  | WXT emits `content-scripts/gmail.js` scoped only to `https://mail.google.com/*`; no `<all_urls>` or main-world injection.                                                                                                                  |
| Thread actions         | PASS (local)                  | Shadow DOM `✦ Subzero` cluster exposes summary, draft, Open Loops, reminder, Ask, side-panel, and Open in Subzero actions; fixture lifecycle test proves one mount after repeated render. Live Gmail toolbar placement remains unverified. |
| Inline summary         | PARTIAL (local)               | Background `ai/summarize` boundary and source-backed summary card are wired. Cache is keyed by thread in the content layer, not yet invalidated by latest-message ID in embedded mode.                                                     |
| Composer integration   | PASS (local)                  | Compose-adjacent `Draft with Subzero` action sends only a typed background request and inserts returned text after user intent; never sends mail. Live Gmail composer behavior remains manual.                                             |
| Quick replies          | PASS (local)                  | Gmail compose Shadow DOM shows three restrained intent chips; each uses the typed background draft path and leaves the result editable. Live Gmail composer behavior remains manual.                                                       |
| Side Panel             | PASS (local)                  | `sidepanel.html` builds with `Now`, `Ask`, `Open Loops`, source chips, current-thread polling, preferences, and theme continuity. Clean-profile Gmail context proof remains manual.                                                        |
| Ask Inbox              | PASS (local)                  | Side Panel uses bounded `ai/ask-inbox` results and source chips; extension live-provider/mailbox proof remains separate.                                                                                                                   |
| Open Loops             | PARTIAL (local)               | Embedded menu triggers existing deterministic scan and Side Panel lists/resolves account-scoped loops; direct single-thread candidate creation is not yet implemented.                                                                     |
| Reminder notifications | PARTIAL (local)               | `chrome.alarms` callback now creates actionable notification buttons when available; multi-reminder scheduling and clean-profile notification clicks remain unverified.                                                                    |
| Focus indicators       | PARTIAL (local)               | Small non-invasive focus signal heuristic exists for unread/important/follow-up rows; deterministic Gmail-row bucket mapping needs live fixture coverage.                                                                                  |
| Settings/preferences   | PASS (local)                  | Onboarding and Side Panel persist experience mode, thread actions, compose AI, loop suggestions, reminders, auto-label, and auto-archive toggles through the typed extension state contract.                                               |
| Command palette        | PASS (local)                  | Gmail-scoped `Cmd/Ctrl+Shift+.` opens a Shadow DOM command surface and avoids editable targets. Live shortcut conflict audit remains manual.                                                                                               |
| DOM resilience         | PASS (local) / UNKNOWN (live) | Central selectors, route/compose derivation, debounced `MutationObserver`, interval route reconciliation, idempotent mounts, sender validation, and teardown are covered locally. Gmail DOM chaos against the real site is not claimed.    |
| Chrome Store readiness | BLOCKED / MANUAL              | Restricted `gmail.modify` review, clean-profile consent/revoke, final permission/data-safety review, signing, submission, and store result remain manual blockers.                                                                         |
